命令语法规则
本书中用于表示命令语法的规则同IOS命令参考一致。命令参考中对这些规则的描述如下。
粗体字代表输入的是命令或关键字。在实际配置例子和输出（不是一般的命令语法）中,粗休字代表用户手工输入的命令(如show命令）。
斜体字指参数,用户需为它们提供实际值。
竖线(|）用于分割可选的、互斥的选项。
中括号（[])表示可选元素。
大括号{}）表示必选项。
中括号内的大括号([{}]）表示必须在可选元素中必须选择一个。
